Inicio
    1. Inicializar una variable llamada "text" como una cadena vacía.
    2. Para cada número "i" desde 0 hasta 9, hacer:
        a. Concatenar la cadena "The number is " con el valor de "i" y agregar "<br>" al final.
        b. Guardar el resultado en la variable "text".
    3. Establecer el contenido del elemento con el id "ciclo" en el documento HTML con el valor de la variable "text".
Fin
